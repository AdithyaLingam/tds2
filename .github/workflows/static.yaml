name: Hello World Workflow

on:
  push:
    branches:
      - main  # This triggers the action on a push to the 'main' branch.

jobs:
  test:
    runs-on: ubuntu-latest  # The job runs on an Ubuntu virtual machine.

    steps:
      - name: Print "Hello, world!"
        run: echo "Hello, world!"  # This prints the desired "Hello, world!" message.

      - name: Print the custom email
        run: echo "24f1002079@ds.study.iitm.ac.in"  # This prints your email message.
